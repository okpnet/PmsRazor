@inherits QualRazorComponentBase
@typeparam TModel
<CascadingValue Name="TableContentParent" Value="this">
    <table @attributes="MergedAttributes">
        <thead>
            <tr>
                @foreach (var column in _columns)
                {
                    @column.RenderHeader()
                }
            </tr>
        </thead>
        <tfoot>
            <tr>
                <th colspan="@NumberOfColumn">
                    @PageInforamation
                </th>
            </tr>
        </tfoot>
        <tbody>
                @foreach (var row in ViewModel.Data.Records.Select(t=> new TableRowState<TModel>(t)))
                {
                    <RowContent Row="row"></RowContent>
                }
        </tbody>
    </table>
</CascadingValue>