@inherits RazorCore
@typeparam TModel

<table class="table is-hoverable is-fullwidth">
    <thead>
        <tr>
            @foreach (var column in Columns)
            {
                <th>@((MarkupString)column.GetColumnNameInvoke())</th>
            }
        </tr>
    </thead>
    <tbody>

    </tbody>
</table>